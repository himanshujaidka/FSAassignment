server:
  port: 8082

spring:
  application:
    name: equipment-service

  datasource:
    url: jdbc:mysql://localhost:3306/equipment_service_db
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  profiles:
    active: dev