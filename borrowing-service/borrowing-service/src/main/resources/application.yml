server:
  port: 8083

spring:
  application:
    name: borrow-service

  datasource:
    url: jdbc:mysql://localhost:3306/borrow_service_db
    username: root
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect

  profiles:
    active: dev